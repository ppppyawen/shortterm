1  安装模板 
2 运行  npm run dev
3 修改  电商 分类列表 特惠商品 精品 
4 先写  分类列表  组件  List.vue
5 数据流程   在main.js主文件里  created方法里 读娶后台，
写入前端本地存储
6 在每个组件里，可以获取到本地存储里的数据，按分类筛选
，渲染到页面上
7 给动态列表添加点击事件，跳转到详情页面。
8。 做下差异化  美化  修饰  首页完成  
81  对 app 整体颜色风格   调整  使用了less 比css 更为强大的样式处理器，
预编译器 最终编译成css 规则  style  lang="less"
变量名  @abc:value;
9  打包命令： 确保 当前项目下   npm  run build  就会生成一个文件夹  dist 
10 正确运行打包后的网页文件  index.html  打开  里面的路径  
/static    =====  ./static
然后在服务器下运行  over.

购物车  
简化版  购物车
排版  列表  先静态
再加vue对象，数据 空的数组 
第两步：点击 添加 点击事件 （很多个）@click   方法 vue   methods 

项目里的购物车

数组  store.js   state   card
添加到购物车，商品的详情页面 点击添加  操作state  card
删除   也是操作的    state  card

上拉刷新 功能

选择swiper  通用化     
源码里  找到上下滚动的效果

上拉刷新   新内容
随机出来的，
再一次向后台发起请求，拿到更新的数据 2条
一次性后台向前端返回100条 初始化只显示20条 ，每次刷新 加两条

从列表 跳转到详情组件 
1 后台获取尽可能多的数据  存入前端仓库  根据序号 id 查询到对应的数据 ，显示。
2 数据量不太大的情况下，直接地址栏传递参数  数据 转为字串  挂在地址栏后面 ，可以获取 
3 从列表页面，点击跳转，传递参数，仅仅传递id号， 在详情页面，根据 id号，再次读取后台 ，拿到数据并渲染。
















